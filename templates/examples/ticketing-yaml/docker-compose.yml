# Ticketing Integration Deployment (YAML forwarding)
# ==================================================
#
# Start: docker-compose up -d
# Logs:    docker-compose logs -f
# Stop:    docker-compose down

services:
  cloud-connector:
    image: ghcr.io/serviceware/cloud-connector:3.0.0
    container_name: ticketing-cloud-connector
    restart: always
    ports:
      - "8080:8080"
    environment:
      # WebSocket URL for the Serviceware Cloud
      - CLOUD_CONNECTOR_WS_URL=${SERVICEWARE_WS_URL}
      - CLOUD_CONNECTOR_HOST=${SERVICEWARE_HOST}
      - CLOUD_CONNECTOR_CLIENT_ID=${SERVICEWARE_CLIENT_ID}
      - CLOUD_CONNECTOR_CLIENT_SECRET=${SERVICEWARE_CLIENT_SECRET}
      - CLOUD_CONNECTOR_LOG_LEVEL=${SERVICEWARE_LOG_LEVEL:-info}
      - OUTBOUND_URL_ALLOWLIST=${OUTBOUND_URL_ALLOWLIST:-[]}
      - CLOUD_CONNECTOR_FORWARDING_CONFIG=/config/forwarding.yml

      # Ticketing API configuration referenced by forwarding.yml
      - TICKET_API_URL=${TICKET_API_URL}
      - TICKET_API_TOKEN=${TICKET_API_TOKEN}

    volumes:
      # Single declarative forwarding configuration (read-only)
      - ./forwarding.yml:/config/forwarding.yml:ro

    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
